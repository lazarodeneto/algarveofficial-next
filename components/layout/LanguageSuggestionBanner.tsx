"use client";

/**
 * Language suggestion banner is disabled — only English is supported.
 * Kept as a no-op export so existing imports don't break.
 */
export function LanguageSuggestionBanner() {
  return null;
}
