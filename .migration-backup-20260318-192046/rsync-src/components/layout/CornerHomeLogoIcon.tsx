export function CornerHomeLogoIcon() {
  // Deprecated on public pages: header already provides home navigation.
  // Keep component as a no-op to avoid accidental overlap regressions if imported.
  return null;
}
